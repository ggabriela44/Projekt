<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>Akcje klienta: {{client.name}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <a href="/clients" class="btn btn-secondary mt-3">Powrót do listy klientów</a>
    <h2 class="text-center mb-4">Akcje klienta: {{client.name}}</h2>

    <!-- Szczegóły klienta -->
    <div class="card mb-4">
      <div class="card-body">
        <h5 class="card-title">Szczegóły klienta</h5>
        <p class="card-text"><strong>Nazwa:</strong> {{client.name}}</p>
        <p class="card-text"><strong>Adres:</strong> {{client.address}}</p>
        {{#if client.nip}}
          <p class="card-text"><strong>NIP:</strong> {{client.nip}}</p>
        {{/if}}
        {{#if client.companyOrPerson}}
          <p class="card-text"><strong>Typ:</strong> {{client.companyOrPerson}}</p>
        {{/if}}
      </div>
    </div>

    <form action="/actions/add" method="POST">
      <input type="hidden" name="clientId" value="{{client._id}}">
      <button type="submit" class="btn btn-primary mb-3">Dodaj Akcję</button>
    </form> 

    
    <table class="table table-bordered">
      <thead class="table-dark">
        <tr>
          <th>Data kontaktu</th>
          <th>Rodzaj akcji</th>
          <th>Opis akcji</th>
        </tr>
      </thead>
      <tbody>
        {{#each actions}}
          <tr>
            <td>{{this.dataKontakt}}</td>
            <td>{{this.rodzajAkcji}}</td>
            <td>{{this.opisAkcji}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>

  </div>
</body>
</html>
