<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>Dodaj Akcję do Klienta</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <a href="/clients" class="btn btn-secondary mt-3">Powrót do listy klientów</a>
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <h2 class="text-center mb-4">Dodaj Akcję do Klienta: {{client.name}}</h2>
        {{#if errors}}
          <div class="alert alert-danger">
            <ul>
              {{#each errors}}
                <li>{{this}}</li>
              {{/each}}
            </ul>
          </div>
        {{/if}}
        <form action="/actions/save" method="POST">
          <input type="hidden" name="clientId" value="{{client._id}}">
          <div class="mb-3">
            <label for="dataKontakt" class="form-label">Data kontaktu</label>
            <input type="date" class="form-control" id="dataKontakt" name="dataKontakt" required  value="{{dataKontakt}}">
          </div>
          <div class="mb-3">
            <label for="rodzajAkcji" class="form-label">Rodzaj akcji</label>
            <select class="form-select" id="rodzajAkcji" name="rodzajAkcji" required>
              <option disabled selected>Wybierz</option>
              <option value="Telefon" {{#ifEquals rodzajAkcji "Telefon"}}selected{{/ifEquals}}>Telefon</option>
              <option value="Spotkanie" {{#ifEquals rodzajAkcji "Spotkanie"}}selected{{/ifEquals}}>Spotkanie</option>
              <option value="Inne" {{#ifEquals rodzajAkcji "Inne"}}selected{{/ifEquals}}>Inne</option>
            </select>
          </div>
          <div class="mb-3">
            <label for="opisAkcji" class="form-label">Opis akcji</label>
            <textarea class="form-control" id="opisAkcji" name="opisAkcji" rows="4" required>{{opisAkcji}}</textarea>
          </div>
          <button type="submit" class="btn btn-primary w-100">Dodaj Akcję</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
